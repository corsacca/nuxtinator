# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
.vscode

# Extended layers (downloaded by c12/giget)
.layers
docs
# Anchored to the repo root: an unanchored pattern also matches the layer
# directories named `context/` (components, pages, api routes), which hides
# their files from git and from Tailwind's source scan.
/context

# Test artifacts
test-results/
playwright-report/
playwright/.cache/
tests/e2e/.results/